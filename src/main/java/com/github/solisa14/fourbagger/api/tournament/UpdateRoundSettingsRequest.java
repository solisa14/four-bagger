package com.github.solisa14.fourbagger.api.tournament;

/** Request body for updating round configuration settings within a tournament bracket. */
public record UpdateRoundSettingsRequest(Integer bestOf) {}
